abstract class GlobalConstants {
  static const borderRadius = 10.0;

  static const screenPadding = 15.0;

  static const double mainButtonHeight = 60;

  static const defaultAnimationDuration = Duration(
    milliseconds: 200,
  );

  static const resultDialogTextAnimationDuration = Duration(
    seconds: 1,
  );

  static const resultDialogWordAnimationDuration = Duration(
    seconds: 3,
  );
}
